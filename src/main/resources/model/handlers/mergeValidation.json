{
  "name": "MergeValidation",
  "tooltip": "A `MergeValidation` defines a query to be evaluated before merging.<br>If the query result is `true`, the merge may continue.<br>If the query result is `false`, the merge is aborted.",
  "attributes": {
    "context": {
      "node": "value",
      "description": "context (only value right now)",
      "tooltip": "The context in which the validation query is executed",
      "values": {
        "current": "execute in the current context"
      },
      "required": true
    },
    "query": {
      "description": "ODT query",
      "tooltip": "If the query result is true, the merge may continue. If false, the merge is aborted",
      "type": "boolean",
      "node": "scalar",
      "required": true
    }
  }
}
